# Core dependencies for InboxHero

# Environment variable management
python-dotenv==1.0.1

# Google Generative AI SDK
google-generativeai==0.5.0

# JSON handling and utilities (standard library covers most, but safe to include)
simplejson==3.19.2

# Testing and linting (optional but recommended)
pytest==8.2.0
flake8==7.1.0
